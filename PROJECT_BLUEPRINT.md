# AI Skills 工作区 — 项目蓝图

> 蓝图规范版本：2
> 文档定位：定义所有技能共同遵守的工作区契约、模块边界、架构红线和待开发工作包索引。
> 事实来源：技能行为以各技能的 `SKILL.md` 和直接引用资源为准；功能设计以 `docs/design/` 对应工作包为准；校验行为以脚本和测试为准。
> 源码边界：`/home/nika/workspace/ai/skills` 是技能源码的唯一可写来源，个人技能目录只保留指向本工作区的发现入口。

## 1. 项目定位

### 1.1 目标

- 集中保存、维护和演进可复用 AI 技能，避免个人安装目录与开发目录形成两个可写副本。
- 为每个技能提供明确触发、核心工作流、渐进式参考资料、输出模板和必要的确定性工具。
- 统一技能源码、发现入口和验证边界，使每个技能可以独立维护、加载和验证。
- 在技能进入可发现状态前验证目录结构、元数据、引用、脚本和关键正反行为。
- 使用紧凑蓝图保存工作区公共约束，使用史诗设计文档隔离单个技能或功能的上下文。

### 1.2 非目标

- 本期不建设技能市场、远程发布平台、在线托管服务或跨机器同步服务。
- 本期不提供通用插件框架、MCP 服务注册中心或第三方技能审核平台。
- 本期不自动扫描、复制或删除工作区外的其他个人技能。
- 本工作区不保存账号凭据、访问令牌、生产数据或用户隐私数据。
- 蓝图不记录修改历史，也不重复可从技能源码、设计文档和脚本恢复的功能细节。

### 1.3 用户与关键场景

| 用户/角色 | 项目级关键场景 | 成功结果 |
|-----------|----------------|----------|
| 技能使用者 | 调用已发现的技能完成其声明范围内的任务 | 获得符合技能契约且不越权的结果 |
| 技能维护者 | 创建、修改、验证和迁移技能 | 工作区保留唯一源码，修改可验证且不破坏发现入口 |
| Codex 运行时 | 根据技能元数据发现并加载技能 | 触发正确，引用可读，资源和脚本可按约定使用 |
| 代码审查者 | 检查技能行为、边界和验证证据 | 能从蓝图、设计、源码和测试判断变更是否可接受 |

## 2. 技术栈

| 类别 | 当前事实或硬约束 | 唯一事实来源 |
|------|------------------|--------------|
| 技能说明 | Markdown 与 YAML frontmatter | 各技能 `SKILL.md` |
| UI 元数据 | YAML | 各技能 `agents/openai.yaml` |
| 规范与模板 | Markdown | 各技能 `references/` 与 `assets/` |
| 确定性校验 | Python 3 标准库 | 各技能 `scripts/` |
| 自动发现 | 本地文件系统符号链接 | 个人技能目录的链接状态 |

## 3. 代码结构

```text
PROJECT_BLUEPRINT.md                 工作区项目级公共契约和待办索引
docs/design/                         已确认或实施中的史诗功能设计
skill-name/
├── SKILL.md                         必需的技能入口和资源路由
├── agents/openai.yaml               可选的 UI 元数据
├── references/                      可选的按需规范和领域知识
├── assets/                          可选的输出模板和静态资源
└── scripts/                         可选的确定性工具和验证脚本
```

不在根目录复制技能内部规范；新增技能必须使用独立顶层目录。

## 4. 模块架构

| 模块 | 职责 | 不得承担 | 依赖方向 |
|------|------|----------|----------|
| Workspace Governance | 定义技能共同约束、开发边界和验收 | 重复保存单个技能的功能设计 | 约束所有技能包 |
| Skill Instructions | 定义触发条件、核心工作流和资源路由 | 承载不相关教程和历史记录 | 引用本技能直接资源 |
| Interface Metadata | 提供技能列表名称、简介和默认提示 | 改写技能真实行为或引入隐藏权限 | 与技能说明保持一致 |
| References | 保存按需加载的详细规范和领域知识 | 与入口重复保存同一规则 | 被 `SKILL.md` 直接路由 |
| Assets | 提供生成结果使用的模板和静态资源 | 作为隐藏指令或运行状态存储 | 由技能在生成阶段读取 |
| Scripts | 提供可重复、确定性的检查或转换 | 静默联网、保存凭据或吞掉失败 | 读取文档并返回明确状态 |
| Validation | 检查结构、引用和关键行为 | 通过放宽规则把无效结果判为成功 | 验证技能说明、资源和输出 |

## 5. 跨模块契约

### 5.1 业务规则与核心不变量

- 每个技能必须是独立目录并包含有效 `SKILL.md`；目录名、frontmatter 名称和默认提示中的技能名必须一致。
- `SKILL.md` 只保留核心工作流和资源路由；详细规范、领域知识和长示例使用一层直接引用的参考文件。
- 反复执行且需要确定性的操作应使用脚本；作为输出起点的内容应使用资产模板。
- 技能不得创建不参与执行的 README、安装指南或变更日志。
- 用户交互类技能必须区分用户原意、已确认结论、证据推断和待确认差量，不得把推断伪装成要求。
- 用户可见回复必须与后台推理、子代理状态和工具过程隔离。
- 项目蓝图只保存项目级公共约束；具体功能设计必须进入 `docs/design/`，待办通过稳定锚点引用工作包。
- 用户指定外部参考项目时，参考事实、适配建议与已确认项目要求必须分离。

### 5.2 发现、引用与状态契约

- 个人技能目录中的工作区技能入口必须解析到本工作区的同名技能包，不得指向第二份实体源码。
- 发现入口目标不存在、不是目录或缺少有效技能说明时，技能视为不可发现，不得报告安装或迁移成功。
- 同一个技能不得同时存在工作区源码和个人目录实体副本。
- 技能名必须少于 64 个字符，并与目录名完全一致。
- 存在 `agents/openai.yaml` 时，其字符串必须显式引用，默认提示中的技能名必须与当前技能一致。
- 参考文件必须由技能说明直接路由并说明读取条件；脚本必须通过退出状态区分成功和失败。
- 蓝图中的非“待澄清”设计依据必须解析到 `docs/design/` 下真实文件的显式锚点。
- 工作包完成后必须从蓝图待开发表删除；设计文档继续保留并更新生命周期状态。

### 5.3 失败与资源边界

- 目标目录已存在且内容不一致时必须停止并报告冲突，不得自动合并或覆盖。
- 技能迁移、重命名或发现入口切换失败时，唯一源码必须保持可恢复，不得留下第二份实体源码。
- 技能验证不得修改被验证文件；验证中断或失败不得留下“已通过”状态。
- 用户已有文件和未提交改动必须保留，结构升级不得改写既有待办语义。
- 脚本产生的缓存和临时夹具必须在验证后清理，不得进入事实来源。
- 同一技能不得由多个进程同时执行结构性重写；发现并发修改时必须停止并重新读取现状。
- 新增外部依赖、网络访问或工作区外写入时，必须先确认权限并定义超时、取消和清理语义。

### 5.4 安全与非功能要求

| 维度 | 项目级要求 | 测量条件 |
|------|------------|----------|
| 上下文效率 | 单个 `SKILL.md` 少于 500 行，条件性规范按需加载 | 执行行数与直接引用检查 |
| 确定性 | 相同文档输入得到相同校验结论 | 连续运行相同正反用例比较退出状态 |
| 可移植性 | 未另行声明的校验脚本只依赖 Python 标准库 | 在无项目第三方包的 Python 3 环境运行 |
| 可维护性 | 同一规则只有一个事实来源，引用不得断裂 | 检查重复规则、相对路径和锚点 |
| 完整性 | 结构验证、脚本语法和代表性正反用例全部通过 | 每次技能完成前执行验证清单 |
| 数据安全 | 源码、模板、示例和测试不得包含凭据或真实生产数据 | 内容扫描和 Review |

### 5.5 开发边界

#### 本期必须实现

- 新增或修改的技能必须保持唯一源码、有效元数据和可验证的发现入口。
- 条件性长规范必须按需加载，反复执行的确定性行为必须由脚本或测试验证。
- 工作区公共约束、功能设计和实现事实必须保持分层，待办只索引尚未完成的工作包。
- 每次变更必须完成与风险相称的结构、引用、脚本和正反行为验证。

#### 明确不做

- 不初始化或绑定远程 Git 仓库，除非用户另行授权。
- 不建设技能市场、自动发布、远程执行、遥测或用户账号系统。
- 不自动安装、更新或删除工作区之外的其他无关技能。
- 不引入第三方运行时依赖、外部服务或新的基础设施组件。
- 不把单个功能的详细设计或代码可恢复信息复制进蓝图。

#### 后续候选

- 仅包括第 6 节待开发功能表中的未完成工作包；候选项在完成澄清前不获得实施授权。

#### AI 可自行决定

- 不改变触发、外部行为、安全和数据语义的技能内部排版与命名。
- 校验脚本内部函数、错误收集和输出排序方式。
- 参考资料的章节拆分，只要保持直接路由和单一事实来源。
- 测试夹具内容，只要覆盖已确认的正常与失败契约且不包含敏感数据。

#### 必须再次确认

- 删除或重命名技能、改变触发描述或公开调用方式。
- 引入第三方依赖、网络服务、凭据、遥测或工作区外写入。
- 初始化远程仓库、选择许可证、公开发布或接入技能市场。
- 改变个人目录符号链接策略、移动工作区或删除唯一源码。
- 放宽验证标准、改变开发边界或执行不可逆批量迁移。

### 5.6 项目级验收

- 每个可发现技能只能解析到一份实体源码，名称、目录、frontmatter 和界面元数据必须一致。
- 新增或修改的技能必须通过官方结构校验及其声明的脚本、引用和行为验证。
- 蓝图规范版本、一级结构、待办表格、设计文件、显式锚点和生命周期必须通过校验。
- 用户可见结果不得泄露内部推理、代理状态、工具草稿、凭据或生产数据。
- 工作区外无关文件和既有项目改动必须保持不变。

## 6. 待开发功能

以下 5 个旧待办仅完成结构迁移，原名称、优先级、顺序和完成定义保持不变；本次未澄清其设计与依赖。

| 编号 | 优先级 | 功能 | 设计依据 | 前置依赖 | 完成定义 |
|------|--------|------|----------|----------|----------|
| PEND-001 | P1 | 版本控制基线 | 待澄清 | 待澄清 | 用户确认后初始化仓库，明确忽略规则、提交规范和恢复方式 |
| PEND-002 | P2 | 工作区统一验证入口 | 待澄清 | 待澄清 | 一条本地命令可发现全部技能并分别执行结构与行为校验 |
| PEND-003 | P2 | 技能目录索引 | 待澄清 | 待澄清 | 自动生成技能名称、用途、入口和验证状态，不复制技能正文 |
| PEND-004 | P3 | 持续集成 | 待澄清 | 待澄清 | 在受控环境执行无网络的结构、语法和正反用例检查 |
| PEND-005 | P3 | 安装与回滚工具 | 待澄清 | 待澄清 | 安装入口切换可验证、可回滚，且不覆盖用户已有技能 |

### 6.1 项目级待确认事项

1. 是否初始化 Git 仓库，以及远程托管位置、分支策略和提交要求。
2. 工作区及其中技能采用何种许可证，哪些内容允许公开分发。
3. 后续技能是否全部使用符号链接安装，是否需要版本化发布目录。
4. 是否需要支持 Linux 之外的操作系统和不同 Codex 技能目录布局。
5. 统一验证入口的性能目标、CI 环境和前向测试隔离方式。

## 7. 系统架构

### 7.1 分层与依赖

```text
工作区蓝图
  ↓ 约束
技能说明 → 按需参考 / 输出模板 / 校验脚本
  ↓ 暴露
UI 元数据与个人目录发现入口
```

技能之间默认不得隐式依赖；组合能力必须声明调用关系、失败传播和兼容范围。

### 7.2 源码与发现分治

- 工作区保存唯一可写源码；个人目录只保存指向工作区的符号链接。
- 设计文档保存功能决策；技能说明只路由到当前任务需要的规范。
- 临时备份和测试夹具不得成为长期事实来源。

### 7.3 运行与恢复

- 结构验证先检查工作区路径，再从发现入口路径复验。
- 链接切换失败时必须恢复到最近验证通过的唯一源码。
- 校验失败必须返回非零状态和可定位错误，不能以警告或空结果伪装成功。

### 7.4 架构红线

- 不得在个人技能目录维护第二份可编辑实体源码。
- 不得把条件性长规范全部塞入 `SKILL.md` 或项目蓝图。
- 不得通过放宽校验、吞掉错误或改写旧待办语义来完成迁移。
- 不得在未取得用户授权时新增外部依赖、网络访问或工作区外写入。
