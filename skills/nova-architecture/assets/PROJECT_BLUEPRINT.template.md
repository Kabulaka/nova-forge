# <项目名称> — 项目蓝图

> 文档定位：定义所有开发长期共同遵守的项目级事实、唯一开发导航，以及用户明确要求沉淀的未完成工作。

## 1. 项目定位

| 对象 | 问题 | 成功结果 |
|------|------|----------|
| <首要使用者或服务对象> | <项目解决的核心问题> | <可验证的项目级结果> |

## 2. 技术栈

| 类别 | 当前事实或硬约束 | 事实来源 |
|------|------------------|----------|
| <类别> | <技术或已确认约束> | <代码、配置或用户确认> |

## 3. 代码结构

### 顶层目录

```text
<稳定的顶层目录与职责>
```

### 代码落位规则

| 代码区域 | 职责 | 代码落位规则 |
|----------|------|--------------|
| <目录> | <唯一职责> | <什么代码应放这里> |

## 4. 模块职责

| 模块 | 职责 | 对外边界 | 允许依赖 |
|------|------|----------|----------|
| <模块> | <唯一职责> | <向其他模块暴露什么> | <允许依赖的模块或层> |

## 5. 公共约束

### 全局契约

| 契约 | 适用范围 | 验证 |
|------|----------|------|
| <稳定规则> | <适用模块> | <可执行验证> |

### 数据、资源与恢复

| 资源或失败点 | 所有者 | 不变量 | 恢复与清理 |
|--------------|--------|--------|------------|
| <数据或资源> | <负责模块> | <始终成立的约束> | <失败后的状态和责任> |

### 开发决策边界

| 边界 | 内容 |
|------|------|
| 必须遵循 | <项目级硬边界> |
| 明确不做 | <不得自行扩展的范围> |
| AI 可自行决定 | <不改变外部行为的内部选择> |
| 必须再次确认 | <会改变共享架构的事项> |

## 6. 开发导航

> 本章是唯一开发发现入口。先按“开发触发条件”判断本次任务是否命中，只读取命中行的精确入口；具体实现仍以代码、测试、迁移和配置为准。

| 开发触发条件 | 名称 | 类别 | 精确入口 | 复用或遵循边界 | 验证入口 |
|--------------|------|------|----------|----------------|----------|
| <数据库连接、事务、迁移或恢复> | <数据访问基座> | 已实现能力 | <仓库根目录相对源码路径> | <只复用公共机制；模块表、查询和 Repository 仍由模块所有> | <测试或配置路径> |
| <涉及某项跨模块公共契约的场景> | <稳定契约名称> | 详细共享契约 | <精确链接到 `.nova/architecture/<topic>.md`> | <判断相关性所需的一句话边界，不复制正文> | <契约验证路径> |

没有条目时写“无”。类别只使用“已实现能力”或“详细共享契约”；未完成候选不得登记。

## 7. 待开发工作

| 开发触发条件 | 待开发内容 | 交付结果 | 设计入口 |
|--------------|------------|----------|----------|
| <本次请求涉及的业务能力、模块或场景> | <稳定、可读的工作名称> | <完成后可观察到的结果> | [设计章节](design/YYYY-MM-DD_<名称>.md#<章节>) |

> 没有条目时删除表格并写“当前无待开发工作”。只有用户明确要求设计、规划或沉淀待办时才增加条目；实现完成后删除对应行，不记录状态、负责人、优先级、排期、进度、Review 或审计信息。
