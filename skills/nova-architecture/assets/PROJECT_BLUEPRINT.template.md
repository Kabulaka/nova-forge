# <项目名称> — 项目蓝图

> 文档定位：定义所有开发长期共同遵守的项目级事实、唯一开发导航，以及用户明确要求沉淀的未完成工作。

## 1. 项目定位

| 对象 | 问题 | 成功结果 |
|------|------|----------|
| <首要使用者或服务对象> | <项目解决的核心问题> | <可验证的项目级结果> |

## 2. 技术栈

| 类别 | 当前事实或硬约束 | 事实来源 |
|------|------------------|----------|
| <类别> | <技术或已确认约束> | <代码、配置或用户确认> |

## 3. 代码结构

### 顶层目录

```text
<稳定的顶层目录与职责>
```

### 代码落位规则

| 代码区域 | 职责 | 代码落位规则 |
|----------|------|--------------|
| <目录> | <唯一职责> | <什么代码应放这里> |

> 只列项目特有且长期稳定的根目录、所有权和落位边界；框架通用分层不能约束本项目实现时不写。

## 4. 模块职责

| 模块 | 职责 | 对外边界 | 允许依赖 |
|------|------|----------|----------|
| <可映射到精确代码或运行体的稳定模块> | <唯一职责> | <向其他模块暴露什么> | <实际允许依赖的模块或层> |

> 只记录模块所有权、对外边界和真实依赖方向；模块必须映射到精确代码目录或运行体，不得用“相关模块”“相关代码”等模糊占位，也不得用数据流、下游消费者或无法映射到实现的人工编号代替“允许依赖”。无法确认细分目录时合并到已知的更大稳定代码区域，不扫描目录补表。单个功能的流程、内部状态、算法和验收属于 Development。

## 5. 公共约束

### 全局契约

| 契约 | 适用范围 | 验证 |
|------|----------|------|
| <稳定规则> | <适用模块> | <可执行验证> |

### 数据、资源与恢复

| 资源或失败点 | 所有者 | 不变量 | 恢复与清理 |
|--------------|--------|--------|------------|
| <数据或资源> | <负责模块> | <始终成立的约束> | <失败后的状态和责任> |

> 公共约束必须长期约束项目整体或两个以上独立参与方，一行只表达一个不变量，并在唯一权威位置更新而不是追加同义或历史版本。涉及数据库、缓存、并发、安全或恢复本身不足以成为架构；通用工程规范留在项目规则、代码或测试，单个功能的事务、重试和失败步骤写入开发设计。

> “数据、资源与恢复”只保留不能由所有者模块自行封装的跨参与方不变量；单模块任务领取、迟到结果、重试、摄取恢复和临时资源清理不写，也不得把这些内容改名后移入“全局契约”。没有合格内容时删除本小节和表格。

### 开发决策边界

| 边界 | 内容 |
|------|------|
| 必须遵循 | <项目级硬边界> |
| 明确不做 | <不得自行扩展的范围> |
| AI 可自行决定 | <不改变外部行为的内部选择> |
| 必须再次确认 | <会改变共享架构的事项> |

> “开发决策边界”只记录本项目特有决定，不复述 Architecture 或 Development 技能的通用职责与路由规则；没有合格内容时删除本小节和表格。

## 6. 开发导航

> 本章是唯一开发发现入口。先按“开发触发条件”判断本次任务是否命中，只读取命中行的精确入口；具体实现仍以代码、测试、迁移和配置为准。

| 开发触发条件 | 名称 | 类别 | 精确入口 | 复用或遵循边界 | 验证入口 |
|--------------|------|------|----------|----------------|----------|
| <数据库连接、事务、迁移或恢复> | <数据访问基座> | 已实现能力 | <仓库根目录相对源码路径> | <只复用公共机制；模块表、查询和 Repository 仍由模块所有> | <测试或配置路径> |
| <项目整体或两个以上独立参与方必须保持兼容的场景> | <稳定契约名称> | 详细共享契约 | <精确链接到 `.nova/architecture/<topic>.md`> | <所有权、接口、数据、运行或恢复边界；不描述功能实现> | <契约验证路径> |

没有条目时写“无”。类别只使用“已实现能力”或“详细共享契约”；未完成候选不得登记。实现文件、启动脚本或配置本身不能同时充当验证入口。升级迁移只迁移旧蓝图或固定旧入口已经明确登记的条目，事实核验过程中发现的代码、配置和测试不得自动晋升为导航。详细共享契约必须是多个独立参与方直接消费的精确版本化接口、数据或事件契约，验证入口指向现有契约、Schema 或可执行校验；框架生成文档的 `info.version` 本身不构成版本化公共契约，不能直接作为某个功能的实现说明、恢复矩阵或未来测试清单，也不能代替待开发工作。空白项目不得为了填充本章而创建详细架构文档。

## 7. 待开发工作

| 开发触发条件 | 待开发内容 | 交付结果 | 设计入口 |
|--------------|------------|----------|----------|
| <本次请求涉及的业务能力、模块或场景> | <稳定、可读的工作名称> | <完成后可观察到的结果> | [设计章节](design/YYYY-MM-DD_<名称>.md#<章节>) |

> 没有条目时删除表格并写“当前无待开发工作”。只有用户明确要求设计、规划或沉淀待办，或 Architecture 将用户明确提出的具体功能限域交给 Development 澄清、设计和登记时才增加条目；设计正文由 Development 维护，Architecture 返回后继续完成架构工作。实现完成后删除对应行，不记录状态、负责人、优先级、排期、进度、Review 或审计信息。
