# <项目名称> — 项目蓝图

> 文档定位：所有任务一次读取的轻量入口；系统骨架、已实现可复用能力和待开发工作同处一份文件，只有系统骨架属于架构。

## 1. 项目定位

| 对象 | 问题 | 成功结果 |
|------|------|----------|
| <首要使用者或服务对象> | <项目解决的核心问题> | <可验证的项目级结果> |

## 2. 技术栈与系统形态

| 类别 | 当前事实或硬约束 | 事实来源 |
|------|------------------|----------|
| <类别> | <技术或已确认约束> | <代码、配置或用户确认> |

## 3. 代码结构

### 顶层目录

```text
<稳定的顶层目录与职责>
```

### 代码落位规则

| 代码区域 | 职责 | 代码落位规则 |
|----------|------|--------------|
| <目录> | <唯一职责> | <什么代码应放这里> |

> 只列项目特有且长期稳定的根目录、所有权和落位边界；框架通用分层不能约束本项目实现时不写。

## 4. 服务与模块骨架

| 服务或模块 | 运行体或代码位置 | 唯一职责 | 允许依赖 |
|------------|----------------|----------|----------|
| <稳定服务或项目级模块> | <精确目录或运行体> | <拥有的系统职责> | <实际允许依赖的服务、模块或层> |

> 只记录系统骨架中的服务/模块、所有权和真实依赖方向；必须映射到精确代码目录或运行体，不得用数据流、业务步骤、下游消费者或人工编号代替模块。无法确认细分目录时合并到已知的更大稳定区域，不扫描目录补表。单个功能经过多个模块不改变本章。

## 5. 系统基础边界

### 存储介质职责

| 介质 | 允许保存 | 禁止作为 | 失效与恢复边界 |
|------|----------|----------|----------------|
| <实际使用的数据库、缓存、文件或日志介质> | <具体的系统级状态类别> | <不得写入或不得成为唯一事实的内容> | <介质失效时必须保持的业务边界> |

> 必须按项目实际介质写清边界，不能只写“业务状态”或“临时状态”。数据库只保存重启后仍必须存在的权威事实、当前有效结果、持久任务状态、稳定关系和资源元数据，不默认保存执行流水、调试轨迹、可重算中间值或每次 AI 临时输出；缓存只保存可从权威来源重建且允许过期的加速数据、会话、锁、租约、幂等或限流键，缓存丢失不得改变业务结果；文件介质保存原始文件、模板和交付物，数据库保存稳定标识与元数据；日志和指标用于诊断，不成为业务真相。项目没有某种介质时不写占位行。

### 运行与全系统边界

| 边界 | 系统级规则 | 适用范围 | 事实来源 |
|------|------------|----------|----------|
| <部署、配置、鉴权、隔离、密钥或系统资源边界> | <所有相关开发必须遵守的一句话规则> | <服务、模块或运行体> | <配置、代码或用户确认> |

> 本章只描述系统地基。业务 API、schema、DTO、数据库表、业务事件、端到端工作流、事务、重试、恢复矩阵和验收属于 Development，即使跨多个模块、有版本或兼容要求也不得写入本章。没有合格内容时删除对应小节，不为模板外观填充。

### 开发决策边界

| 边界 | 内容 |
|------|------|
| 必须遵循 | <项目级硬边界> |
| 明确不做 | <不得自行扩展的范围> |
| AI 可自行决定 | <不改变外部行为的内部选择> |
| 必须再次确认 | <会改变系统骨架的事项> |

> “开发决策边界”只记录会改变系统骨架的项目特有决定，不复述技能通用规则，不写业务范围或功能候选；没有合格内容时删除本小节和表格。

## 6. 已实现可复用能力

> 本章是已落地通用组件的唯一发现入口。只在当前任务相关时读取命中行的实现和验证入口；具体事实仍以代码、测试、迁移和配置为准。

| 复用时机 | 能力 | 实现入口 | 复用边界 | 验证入口 |
|----------|------|----------|----------|----------|
| <当前任务需要使用某项通用机制时> | <已实现并验证的稳定能力> | <仓库根目录相对源码路径> | <允许复用的机制和仍由调用方负责的内容> | <独立测试或可执行校验路径> |

没有条目时写“无”。能力必须已经实现、具备稳定入口和独立验证，并且离开当前单一业务工作流后仍能被无关功能直接复用；两个现有无关消费者，或明确的项目级基础组件，才构成复用证据。潜在复用、单一业务工作流、业务 API/schema/事件、内部辅助代码和未完成候选不得登记。只处理当前任务实际新增、删除或改变的能力，不扫描历史代码补表；实现入口不能同时充当验证入口。

## 7. 待开发工作

| 何时处理 | 待开发工作 | 交付结果 | 设计入口 |
|----------|------------|----------|----------|
| <触发该大块需求或独立技术债务的场景> | <稳定、可读的工作名称> | <完成后可观察到的结果> | 无 / [设计章节](design/YYYY-MM-DD_<名称>.md#<章节>) |

> 没有条目时删除表格并写“当前无待开发工作”。“无”表示尚未沉淀设计，精确链接表示设计已经确认；没有设计链接不强制普通开发先创建设计。只登记用户要求拆分或保存的大块开发需求，以及 Review 最终确认、当前范围不能安全修复且值得独立处理的真实技术债务。同义内容更新原行，完成后删除；不记录状态、来源、负责人、优先级、排期、进度、Review 或审计信息。
